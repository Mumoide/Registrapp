import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prueba2.app',
  appName: 'prueba2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
